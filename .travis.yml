language: cpp
sudo: required

addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
  packages:
    - gcc-4.9
    - g++-4.9

compiler:
  - gcc-4.9

before_script:
  - mkdir build
  - cd build
  - cmake ..

script:
  - make
